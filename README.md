Markdown

# 🎨 Art-IR: Гүнзгий Сургалтад Суурилсан Урлагийн Зургийн Хайлтын Систем (CBIR) (master дээр code байгаа)

Энэхүү төсөл нь Content-Based Image Retrieval (CBIR) зарчим дээр суурилсан бөгөөд Pre-trained Convolutional Neural Network (CNN) моделиудыг ашиглан урлагийн бүтээлүүдийг визуал төстэй байдлаар хайх прототип системийг Streamlit-ээр хэрэгжүүлсэн.

## 🌟 Төслийн Зорилго ба Онцлог

| Ангилал | Дэлгэрэнгүй |
| :--- | :--- |
| **Асуудлын шийдэл** | Хэрэглэгч текстийн мэдээлэлгүйгээр зөвхөн зургаар хайлт хийх боломжийг бүрдүүлэх. |
| **Зорилго** | ResNet50 ба VGG16 CNN-ийн feature-ийн чанар болон Cosine/Euclidean метрикүүдийн гүйцэтгэлийг харьцуулан, шилдэг хайлтын загварыг тодорхойлох. |
| **Үндсэн арга** | **Vector Space Model (VSM)** + **Deep Learning Embeddings**. |

## ⚙️ Ашигласан Технологи ба Сангууд

* **Хэл:** Python 3.8+
* **Deep Learning:** `PyTorch`, `Torchvision` (ResNet50, VGG16)
* **Вектор Тооцоолол:** `NumPy`, `Scikit-learn`
* **Вэб Интерфэйс:** `Streamlit`

## 🧱 Системийн Архитектур ба Бүтэц

### Feature Extraction and Indexing (Индексжүүлэлт)

1.  **Өгөгдөл:** `famous_art_images/` (нийт $\sim 50$ зураг) болон `art.csv` (метадата).
2.  **Урьдчилсан Боловсруулалт:** Зургийг $224 \times 224$ хэмжээтэй болгон ImageNet-ийн стандартаар нормчлох.
3.  **Feature Vector Гаргах:** CNN-ийн Global Average Pooling үеийн дараах гаралтыг ашиглах:
    * **ResNet50:** $D=2048$ хэмжээст вектор.
    * **VGG16:** $D=4096$ хэмжээст вектор.
4.  **$L_2$ Нормчлол:** Бүх векторт $L_2$ нормчлол хийж, нэгж урттай болгох.
5.  **Индекс:** Нормчилсон векторуудыг `features/` хавтсанд `.pkl` файлаар хадгалах.

### Хайлтын Урсгал

Query Image $\to$ Feature Extraction $\to$ $L_2$ Norm $\to$ **Similarity Search** (Индексийн эсрэг) $\to$ **Ranking** (Top-3) $\to$ Streamlit Output.

## 🚀 Ажиллуулах Заавар

### 1. Төслийг Татаж Авах

```bash
git clone [Your Repository URL]
cd Art-IR
2. Шаардлагатай Сангуудыг Суулгах
Bash

pip install torch torchvision numpy scikit-learn pandas streamlit Pillow
3. Feature Vector-уудыг Үүсгэх (Indexing)
Төслийг ажиллуулахын өмнө feature vector-уудыг тооцоолж хадгалах шаардлагатай.

Bash

# src/feature_extractor.py скрипт нь features/ хавтсанд index файлуудыг үүсгэнэ.
python src/feature_extractor.py
4. Системийг Эхлүүлэх (Streamlit Demo)
Дараах командыг ашиглан веб интерфэйсийг эхлүүлнэ:

Bash

streamlit run app.py
